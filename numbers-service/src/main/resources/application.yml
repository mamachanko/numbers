spring:
  application:
    name: numbers-service
  security:
    user:
      password: password
  cloud:
    services:
      registrationMethod: direct

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka